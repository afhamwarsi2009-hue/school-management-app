# Admin Panel Structure

This folder separates admin-only product areas from the public website:

- `dashboard`
- `authentication`
- `fee-management`
- `student-management`
- `teacher-management`
- `payment-history`
- `content-management`

The live React routes are currently implemented in `../frontend/dashboard` and can be expanded into this domain when the admin panel becomes a separate app.
