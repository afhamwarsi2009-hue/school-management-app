# Student Portal Structure

Dedicated student portal modules:

- `dashboard`
- `attendance`
- `results`
- `homework`
- `timetable`
- `downloads`

The live React route is currently implemented in `../frontend/dashboard/StudentDashboard.jsx`.
