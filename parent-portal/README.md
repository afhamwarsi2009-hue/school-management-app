# Parent Portal Structure

Dedicated parent portal modules:

- `dashboard`
- `progress-tracking`
- `attendance`
- `fees-status`
- `notifications`

The live React route is currently implemented in `../frontend/dashboard/ParentDashboard.jsx`.
